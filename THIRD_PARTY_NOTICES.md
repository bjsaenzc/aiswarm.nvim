# Third-party notices

| Component | Notice |
| --- | --- |
| Bundled v2 control-plane body in `bin/aiswarm` | Retains its source header: "License: public domain / CC0. Adapt freely." Added v3 routing and client/runtime code are covered by `LICENSE` (MIT). |
| `runtime/mock-provider` | Adapted from the bundled mock-provider shell case; public domain / CC0 as above. |
| snacks.nvim | External dependency from folke/snacks.nvim, Apache-2.0. It is not vendored or pinned by a lockfile in this standalone checkout. Historical validation used commit `882c996c`. |

See `LICENSE` for the MIT notice. This inventory describes the bundled source notices; it does not assert a separately verified upstream provenance history.
