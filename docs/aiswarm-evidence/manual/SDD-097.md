### SDD-097

- Kind: manual
- Status: passed
- Commit: 7bc8340
- Environment: Neovim 0.12.0-dev-1781, tmux 3.6a, Darwin 25.6.0 arm64 (10 cores), local APFS filesystem, snacks.nvim 882c996c
- Procedure: `bash scripts/bench-aiswarm.sh 600` (the automated SDD-097 case with the exact workload: 10 workers, 1,000 task records, 200 progress records/s and 1 MiB/s output targets, 10 minutes, 60-second attempts, editor workspace open with the Output tab and an external consumer subscribed). Percentiles use nearest-rank over all samples; timestamps are monotonic-wall milliseconds from the record's emission (before the writer's ≤100 ms flush), so the figures are conservative.
- Expected: p95 flushed→visible ≤ 500 ms, p95 flushed→consumer ≤ 500 ms, cached open p95 ≤ 100 ms, UI batch p95 ≤ 16 ms, worker RSS within the ADR 0001 budget, editor memory bounded, achieved load near the target; a slower run fails.
- Actual: third full run (600.4 s): achieved 192.4 records/s aggregate (115,487 progress records received by the editor, 119,563 by the consumer); p95 flushed→store 274 ms, →visible 291 ms, →consumer 303 ms; cached workspace open p95 69 ms; render batch p95 12.0 ms over 6,639 batches (293,596 marks coalesced); worker RSS 22.4 MiB average, workers' CPU peak 32.7 %; editor RSS 240 MiB (Lua heap 83 MiB); integrity: 0 duplicates, 0 discarded bytes, 143,189 store records evicted by the 2,000-record editor bound with the gap flagged; 90 tasks finished, 10 running at the end. Earlier full runs failed for causes that were fixed and are recorded here: an unbounded undo history on pane buffers (editor RSS 1.5 GiB), dispatch holding the control lock across worker spawns, a tmux respawn race, full-list re-rendering (48 ms batches), and a tick-counting load generator (115 records/s).
- Artifacts: `docs/aiswarm-evidence/ledger.md`
