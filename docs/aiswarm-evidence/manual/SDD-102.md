### SDD-102

- Kind: manual
- Status: unverified
- Commit: 7bc8340
- Environment: Neovim 0.12.0-dev-1781, macOS 25.6.0; no authorized real-provider smoke run available in this environment
- Procedure: a versioned Codex native-event adapter requires the current official CLI documentation or supported introspection for one exact version, recorded fixtures for that version, and an explicitly authorized real-provider smoke run. None of these were available during implementation, and guessing flags from another provider is forbidden by the plan.
- Expected: native progress/tool/usage fields normalized from recorded fixtures with raw fallback preserved; smoke run confirms the advertised fields.
- Actual: not implemented. The provider keeps the generic core telemetry (raw output, exit, heartbeat, explicit progress) through the registry's argv construction, and its capability set advertises no native fields (verified by SDD-016 and SDD-074). The task stays unchecked; nothing native is advertised.
- Artifacts: `lua/aiswarm/providers/registry.lua`
