### SDD-099

- Kind: manual
- Status: passed
- Commit: 885cbbc (working tree with the SDD-098/099 fixes, uncommitted)
- Environment: Neovim v0.12.0-dev-1781+g3afe0c6740, tmux 3.6a, jq-1.7.1-apple, GNU bash 3.2.57(1)-release (arm64-apple-darwin25), Darwin 25.6.0 arm64, Apple M1 Pro (10 cores), local APFS, snacks.nvim 882c996cf28183f4d63640de0b4c02ec886d01f2 (lazy-lock.json)
- Procedure: `bash scripts/test-aiswarm.sh --suite <core|compatibility|reliability|performance>` run back to back on the same tree (run ids 20260914T132429Z-46912, 20260914T132647Z-89679, 20260914T132709Z-14964, 20260914T132823Z-85138), after the 10-minute benchmark of SDD-097, the real-terminal captures of SDD-098 and the demonstration of SDD-100. A first pass (run 20260914T131725Z-46982 and 20260914T131943Z-82078) failed three cases; the causes were fixed and are listed below, then every suite was rerun from scratch.
- Expected: every automated suite and manual requirement passes on the supported combination; a missing environment leaves its cell unverified.
- Actual: core 235 passed / 0 failed / 0 unverified (136.9 s); compatibility 61 / 0 / 0 (21.3 s); reliability 50 / 0 / 0 (73.4 s); performance 12 / 0 / 0 (62.8 s). Matrix cells and the cases that prove them: crash injection → `recovery.restart_at_every_crash_point_matches_committed`, `queue.crash_cannot_split_task_and_prompt`, `journal.fault_injection_never_reuses_committed_seq`; retained dead pane → `tmux.finished_pane_retained_and_peekable`; two boards → `tmux.two_boards_same_task_id_coexist`, `matrix.both_names_v2_and_v3_boards`; migration interruption → `migrate.interrupt_resume_and_rollback`, `migrate.backup_verified_and_failure_leaves_source`; restart/ack boundaries → `consumer.durable_delivery_survives_restarts`, `ack.independent_monotonic_validated`, `journal.single_writer_and_generation_on_restart`, `reconnect.backoff_states_and_gap_resync`; storage write failure → `outcome.collector_failure_not_misreported` (injected `write` failures through `AISWARM_FAULT_LOG_WRITES`); rotation → `retention.rotation_eviction_and_gaps`; full benchmark → `bench.reference_load_envelope` and the SDD-097 record.
- Fixed during this task (first pass failures): `matrix.both_names_v2_and_v3_boards` exposed that the v3 engine's `tasks` view was a `__pairs` proxy, which LuaJIT does not honour, so any `pairs()` over `aiswarm.state().tasks` on a v3 board saw an empty table; the store now keeps its `tasks`/`attempts` table identity across resets and snapshots and the engine hands the table out directly. `docs.quickstart_runs_with_mock` parsed the task state with `grep` on JSON whose key order is not stable and could pick the scheduler's state; it now uses `jq`. `cleanup.repeated_cycles_return_to_baseline` counted stream processes machine-wide and saw a stream from a concurrently running demo; the count is now scoped to the editor's children.
- Unverified cells: Linux (no Linux runner in this environment); Neovim 0.10.4 minimum (only the current development build was available, the minimum is documented and health-checked, not executed); real ENOSPC on a full volume (only injected write failures are exercised); power loss (process-crash safety only, as ADR 0002 states). The release gate for those cells stays pending.
- Artifacts: `artifacts/aiswarm/20260914T132429Z-46912/evidence.json`, `artifacts/aiswarm/20260914T132647Z-89679/evidence.json`, `artifacts/aiswarm/20260914T132709Z-14964/evidence.json`, `artifacts/aiswarm/20260914T132823Z-85138/evidence.json`
