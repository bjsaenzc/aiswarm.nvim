### SDD-100

- Kind: manual
- Status: passed
- Commit: 885cbbc (working tree with the SDD-098/099 fixes, uncommitted)
- Environment: as recorded in SDD-099 (macOS arm64, Neovim v0.12.0-dev-1781, tmux 3.6a, jq 1.7.1, bash 3.2.57, snacks.nvim 882c996c)
- Procedure: `bash scripts/validate-aiswarm-plan.sh --allow-missing-artifacts` (every checked task has a passing record and complete dependencies); `docs/aiswarm-release-checklist.md` reconciles each requirement row with its evidence; `bash scripts/aiswarm-demo.sh` demonstrates, from one disposable board, the canonical rename plus legacy name, the task journey (queue → run → cancel → retry → succeeded with report), live logs and progress on `stream`, and an external consumer restarted mid-delivery.
- Expected: every core checkbox has a passing record, no unresolved P0/core defect, the demonstration completes, and nothing incomplete is advertised as core.
- Actual: validator reports 107 tasks, 100 complete, 0 problems (SDD-101–107 carry unverified records and stay unchecked). Demonstration output (exit 0): both names read the same board; `dispatched 1, failed 0, skipped 1` with the dependent task blocked; `cancelled T-001 (attempt 1, cancelled)`; `T-001 queued again (revision 4)`; second dispatch → `T-001 succeeded`; logs show the planning/editing/testing phases and `stream --once` returns 14 progress/lifecycle frames; the consumer run with the `after_accept` fault then a plain rerun accepted 22 events with 22 unique event ids (no duplicate, no loss); `briefing` summarises `T-002 queued; T-001 succeeded`. Optional capabilities remain hidden in the UI and listed under Limits in the docs; no commit or publication is implied.
- Defects found and fixed while closing (recorded in SDD-098 and SDD-099): popups behind the workspace layout, clipped footer hints, a non-iterable v3 task view, and three test-side faults (empty boundary captures, byte-wise glyph check, unstable JSON parsing in the quickstart, machine-wide process count).
- Artifacts: `docs/aiswarm-release-checklist.md`, `docs/aiswarm-evidence/ledger.md`, `docs/aiswarm-evidence/manual/`
