### SDD-098

- Kind: manual
- Status: passed
- Commit: 7bc8340 (working tree with the aiswarm implementation, uncommitted)
- Environment: Neovim 0.12.0-dev+g3afe0c6740, tmux 3.6a, Darwin 25.6.0 arm64, snacks.nvim 882c996c, `nvim --clean -u tests/fixtures/terminal/init.lua` (snacks + plugin only, `ui.motion = false`)
- Procedure: `bash scripts/test-aiswarm.sh --task SDD-098 --keep` runs `scripts/screenshot-aiswarm.sh`, which starts an actual Neovim TUI inside tmux panes of exact sizes on a disposable fixture board (running quiet task, failed task with a missing report, blocked task, queued task, succeeded task with a CJK/emoji title, cancelled task) and captures the rendered screen with `tmux capture-pane` after every keystroke. Sizes: 140×45, 100×30, 80×24, 60×20, 35×10 (dark); 140×45 light; 140×45 ASCII icons without truecolor; breakpoint edges 110×30, 109×30, 110×29. Keystrokes per size, recorded in `index.txt`: `j Enter ] ] ] BSpace x n ? Esc Esc n Esc Esc q`. The automated case checks the essentials; this record is the reviewer pass over the captures of run `20260914T131338Z-68741`.
- Expected: no clipping of essential actions or state, no illegal geometry, selected row readable, wide glyphs intact, no focus or scroll jump; output, report and activity reachable in ≤2 actions from the selected task; a first-time reviewer completes the mock onboarding (select, inspect, tabs, back, cancel prompt, action menu, composer, close).
- Actual: passed on review of every capture listed below.
  - 140×45 dark/light/ASCII: three panes (tasks, inspector, activity tray), full footer `Enter inspect … ? actions   q close`, selected row marked `>`, `日本語 タイトル 🚀` intact in the finished group; `Enter` → inspector on Overview (1 action), `]` → Activity/Output/Report tabs (2 actions), `Backspace` returns to the tasks pane with the selection kept; `x` shows the centred confirm `Cancel T-003 · attempt 1 (running)?` with `y / n`; `?` shows `Actions for T-003` with 15 items, illegal ones annotated (`only queued tasks can be edited`); `n` opens the composer (`Title:`, `--- prompt below this line`); `q` closes and leaves an empty buffer. Light theme renders the same text; ASCII variant uses `* x o + - v >` only (no `● ○ ✓ ✗ ◌ ⊘`).
  - 100×30 and the 110×30 / 109×30 / 110×29 edges: medium layout (tasks + inspector, no tray); the footer picks the medium variant so `? actions  q close` stays visible.
  - 80×24 and 60×20: narrow single-pane layout; footer compacts to `Enter  t out  R rep  n new  x r  ? more  q`; `Enter` navigates into the inspector, `Backspace` returns; the CJK title is untruncated at 60 columns.
  - 35×10: minimal mode explains the 40×12 floor, shows counts and the `p picker  : cmd  q close` footer; no layout assertion, no illegal geometry.
- Issues found by this task and fixed before the passing run (all in the working tree):
  - Confirm, composer and migration popups opened *behind* the workspace: the Snacks layout stacks its floats at zindex 52+ while `Snacks.win` defaults to 50. Headless snapshots could not see this. Fixed with `zindex = Snacks.win.zindex(60)` on those windows (`ui/actions.lua`, `ui/composer.lua`, `ui/migrate.lua`).
  - Footer hints were clipped between roughly 70 and 125 columns (`q close` and `? actions` cut off). `workspace.hints` now chooses the longest of several variants that fits the border line.
  - The three breakpoint-edge captures were empty: a stray argument in `screenshot-aiswarm.sh` made tmux run `dark` as the pane command. Removed.
  - The ASCII assertion used a Lua byte class over multi-byte glyphs and matched the box-drawing border; it now checks each glyph as a plain string.
- Not covered: mouse interaction; terminals other than tmux's built-in emulation; a colour-accurate review (captures are text, highlight groups are checked by SDD-050/053).
- Artifacts: `artifacts/aiswarm/20260914T131338Z-68741/tmp/terminal_screens_at_every_size-screens` (kept run), `docs/aiswarm-evidence/ledger.md`
