### SDD-007

- Kind: manual
- Status: passed
- Commit: 7bc8340
- Environment: Neovim 0.12.0-dev-1781, tmux 3.6a, jq-1.7.1, bash 3.2.57, Darwin 25.6.0
- Procedure: check that the breakpoint function in ADR 0004 is total and single-valued over integer (W,H) (four disjoint guarded branches with a final else), then walk the key sequence Tasks→Inspect→Back, composer Esc, cancel confirmation and view-follow pause listed in §"Key walkthrough".
- Expected: every integer width/height pair maps to exactly one layout; the walkthrough leaves task selection intact and pauses only view following.
- Actual: branches are ordered guards (minimal, narrow, wide, else medium) so exactly one applies; the ≥110×24–27 gap resolves to medium; walkthrough recorded in the ADR. Automated resolver coverage is owned by SDD-053.
- Artifacts: `docs/aiswarm-decisions/0004-layout-and-key-contract.md`
